# MediaMTX binaries

Linux dev: `./scripts/install_mediamtx.sh`

Windows USB kit: bundled automatically by `packaging/make_windows_kit.sh` into
`payload/vendor/mediamtx/mediamtx.exe`.

These binaries are not committed to git (see `.gitignore`).
